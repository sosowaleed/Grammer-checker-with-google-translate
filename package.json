{
  "name": "polyglot-grammar",
  "version": "1.0.0",
  "description": "Cross-browser Manifest V3 extension for real-time grammar checking, contextual synonyms, and multilingual translation powered by Google Translate",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:chrome && npm run build:firefox",
    "build:chrome": "node scripts/build.js chrome",
    "build:firefox": "node scripts/build.js firefox",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^22.13.0",
    "@types/webextension-polyfill": "^0.10.7",
    "happy-dom": "^20.14.0",
    "typescript": "^5.7.3",
    "vite": "^6.1.0",
    "vitest": "^3.0.5"
  }
}
